{
  "permissions": {
    "allow": [
      "WebFetch",
      "WebSearch",
      "Bash(uv run ty:*)",
      "Bash(uv run ruff:*)",
      "mcp__ide__getDiagnostics"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "uv run ruff check --fix && uv run ruff format && uv run ty check",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
